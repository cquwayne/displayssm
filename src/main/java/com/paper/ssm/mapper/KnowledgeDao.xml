<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.paper.ssm.dao.KnowledgeDao">

    <resultMap type="com.paper.ssm.entity.Knowledge" id="KnowledgeMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="impact" column="impact" jdbcType="VARCHAR"/>
        <result property="conditions" column="conditions" jdbcType="VARCHAR"/>
        <result property="envload" column="envLoad" jdbcType="VARCHAR"/>
        <result property="datalist" column="dataList" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="KnowledgeMap">
        select id,
               impact,
               conditions,
               envLoad,
               dataList
        from paper.knowledge
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="KnowledgeMap">
        select id,
               impact,
               conditions,
               envLoad,
               dataList
        from paper.knowledge limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="KnowledgeMap">
        select
        id, impact, conditions, envLoad, dataList
        from paper.knowledge
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into paper.knowledge(impact, conditions, envLoad, dataList)
        values (#{impact}, #{conditions}, #{envload}, #{datalist})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into paper.knowledge(impact, conditions, envLoad, dataList)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.impact}, #{entity.conditions}, #{entity.envload}, #{entity.datalist})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into paper.knowledge(impact, conditions, envLoad, dataList)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.impact}, #{entity.conditions}, #{entity.envload}, #{entity.datalist})
        </foreach>
        on duplicate key update
        impact = values(impact) , conditions = values(conditions) , envLoad = values(envLoad) , dataList =
        values(dataList)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update paper.knowledge
        <set>
            <if test="impact != null and impact != ''">
                impact = #{impact},
            </if>
            <if test="conditions != null and conditions != ''">
                conditions = #{conditions},
            </if>
            <if test="envload != null and envload != ''">
                envLoad = #{envload},
            </if>
            <if test="datalist != null and datalist != ''">
                dataList = #{datalist},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from paper.knowledge
        where id = #{id}
    </delete>

</mapper>